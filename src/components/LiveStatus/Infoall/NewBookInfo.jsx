import { Typography } from "@mui/material";

import React from "react";

const NewBookInfo = () => {
  return (
    <div style={{ display: "flex", flexDirection: "column"  }}>
      12,454
      <Typography variant="subtitle2">New Booking</Typography>
    </div>
  );
};

export default NewBookInfo;
