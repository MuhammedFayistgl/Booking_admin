import { Typography } from "@mui/material";
import React from "react";

const SchedulInfo = () => {
  return (
    <div style={{ display: "flex", flexDirection: "column" }}>
      12,454
      <Typography variant="subtitle2">Schedule Room</Typography>
    </div>
  );
};

export default SchedulInfo;
